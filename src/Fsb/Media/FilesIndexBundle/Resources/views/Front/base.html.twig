{% extends 'FsbMediaFilesIndexBundle::base.html.twig' %}

{% block body %}
    <nav class='navbar navbar-index navbar-default navbar-fixed-top' role='navigation'>
        <div class='container'>
            <!-- Brand and toggle get grouped for better mobile display -->
            <div class='navbar-header'>
                <button type='button' class='navbar-toggle' data-toggle='collapse' data-target='#nav-content'>
                    <span class='sr-only'>Navigation</span>
                    <span class='icon-bar'></span>
                    <span class='icon-bar'></span>
                    <span class='icon-bar'></span>
                </button>
                {% block brand_link %}
                    <a class='navbar-brand' href='{{ path('fsb_media_files_index_homepage') }}'><i class='icon icon-user'></i> {{ app.user.username }}</a>
                {% endblock %}
            </div>
            <!-- Collect the nav links, forms, and other content for toggling -->
            <div class='collapse navbar-collapse' id='nav-content'>
                {% block nav_content %}
                    {% block logout_link %}
                        <ul class='nav navbar-nav navbar-right'>
                            <li>
                                <a href='{{ path('fsb_media_files_index_admin_logout') }}'><span class='icon icon-power-off'></span> Déconnexion</a>
                            </li>
                        </ul>
                        <form class='navbar-form navbar-left'>
                            <input type='search' class='form-control' placeholder='Rechercher' v-model='query' />
                        </form>
                    {% endblock %}
                {% endblock %}
            </div>
        </div>
    </nav>
    <main id='app-container'>
        <section class='container'>{% block app_container %}{% endblock %}</section>
    </main>
{% endblock %}